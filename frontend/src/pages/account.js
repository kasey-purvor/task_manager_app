import Head from "next/head";
export default function Account(user) {
    return (
        <div>
            <Head>
                <title>Account</title>
            </Head>
            <h1>Account</h1>
        </div>
    );
}
