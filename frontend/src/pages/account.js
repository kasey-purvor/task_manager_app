import Head from "next/head";
export default function Account(user) {
    return (
        <div>
            <Head>
                <title>Account</title>
            </Head>
            <h1>Account Page</h1>
        </div>
    );
}
